# Anchor / Rust
.anchor
target
test-ledger
**/*.rs.bk

# Node
node_modules
.yarn
*.tsbuildinfo

# Env / secrets
.env
.env.local
.env.*.local

# Demo keypairs generated by scripts/seed-roles.ts. Devnet-only, but still
# private keys — never commit.
keys/

# OS
.DS_Store
Thumbs.db
